"use client";

// Once rollup supposts 'use client' module directives then 'next-sanity' will include them & this re-export will no longer be necessary.
export { PreviewSuspense as default } from "next-sanity/preview";
